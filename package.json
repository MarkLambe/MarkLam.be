{
  "name": "my-app",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt build --target static && nuxt export",
    "start-prod": "nuxt start"
  },
  "dependencies": {
    "@nuxt/components": "^1.0.5",
    "nuxt": "^2.13.0",
    "vue-lazyload": "^1.3.3"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "sass-loader": "^9.0.2"
  }
}
